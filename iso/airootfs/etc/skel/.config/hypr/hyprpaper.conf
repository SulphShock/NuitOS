# NuitOS default wallpaper (hyprpaper 0.8 syntax)
# Empty monitor = fallback for every output. Randomize later with
# `nuit-random-wallpaper` or hyprctl hyprpaper IPC.

splash = false

wallpaper {
    monitor =
    path = /usr/share/backgrounds/Cat_at_Play.png
    fit_mode = cover
}