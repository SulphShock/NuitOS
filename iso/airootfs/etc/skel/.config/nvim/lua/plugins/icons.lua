-- mc-dp-icons is VSCode-only (no Neovim port yet): devicons here, plus
-- cord.nvim on its minecraft theme, is the closest match.
-- One day, little creeper icons. One day.
return {
  'nvim-tree/nvim-web-devicons',
  lazy = true,
  opts = { default = true, strict = true },
}
