# Copy this dir to modules/<your-id>/ and fill in.
# Every module that ships files MUST be listed in a profiles/*.list to be built.
id=template
version=1
provides=none
requires=
conflicts=
description=Copy-me interface example. See README.
