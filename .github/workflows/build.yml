name: build
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
      options: --privileged
    steps:
      - uses: actions/checkout@v5
      - name: Install archiso
        run: pacman -Sy --noconfirm archiso git diffutils
      - name: Build ISO
        run: |
          export NUIT_WORK=/tmp/nuitos-build
          ./scripts/nuit-release.sh
          ls -lh out/*.iso
          sha256sum out/*.iso | tee out/SHA256SUMS
